<%= form_for(@train_number) do |f| %>
  <% if @train_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@train_number.errors.count, "error") %> prohibited this train_number from being saved:</h2>

      <ul>
      <% @train_number.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
