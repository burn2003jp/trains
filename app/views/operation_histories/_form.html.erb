<%= form_for(@operation_history) do |f| %>
  <% if @operation_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@operation_history.errors.count, "error") %> prohibited this operation_history from being saved:</h2>

      <ul>
      <% @operation_history.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
