<%= form_for(@operation_number) do |f| %>
  <% if @operation_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@operation_number.errors.count, "error") %> prohibited this operation_number from being saved:</h2>

      <ul>
      <% @operation_number.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
